<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %> <%= task.details %>
    <input type="checkbox" value=<%= :completed %> />
</li>
  <%# link_to "delete", task_path(task), method: :delete %>
<%= link_to task.title, task_path(task) %>
  <% end %>
</ul>
